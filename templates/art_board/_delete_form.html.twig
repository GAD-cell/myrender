<form method="post" action="{{ path('app_art_board_delete', {'id': art_board.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ art_board.id) }}">
    <button class="btn">Delete</button>
</form>
