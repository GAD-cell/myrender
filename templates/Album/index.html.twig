{% extends 'base.html.twig' %}

{% block title %}Hello RendusController!{% endblock %}

{% block main %}

<h1>Albums Index</h1>

<table class="table">

    <thead>
        <tr>
            <th>Id</th>
            <th>Description</th>
            <th>Lien</th>
        </tr>
    </thead>
    <tbody>
        {% for album in albums %}
        <tr>
            <td>{{album.id}}</td>
            <td>{{album.description}}</td>
            <td><a href="{{ path('album_show', {'id': album.id}) }}">show</a></td>
        </tr>
        {% endfor %}{# rendus #}


    </tbody>
</table>

    <a href="{{ path('new_album') }}">Create new</a>

{% endblock %} {# main #}
